import { StyleSheet, Text, View } from 'react-native';
import ScreenMainView from 'src/components/lib/ScreenMainView';

interface Props {}

const SwapScreen = (props: Props) => {
	return (
		<ScreenMainView>
			<Text>Swap</Text>
		</ScreenMainView>
	);
};

export default SwapScreen;

const styles = StyleSheet.create({});
